# PDF 数据字典查询 Skill：初版开发计划

版本：v0.1  
日期：2026-09-24  
状态：实施方案；尚未使用公司 PDF 验证。

## 1. 目标与范围

为 Claude Code 建立一个轻量查询 skill，查询指定文件夹内十几份、每份约百页的数据库 PDF 文档，回答字段含义、代码取值、字段所属表及相关业务说明，并给出可核实的来源。

初版采用：**字段定位索引＋原文全文检索＋按需读取＋带来源的回答**。解析和检索在本地执行，命中的资料通过公司现有 Claude Code 环境进入模型上下文。

优先支持：

- 已知表名和字段名，查询定义、类型、取值和文档中的注意事项。
- 已知字段名，查询它在哪些表中出现，并区分不同定义。
- 只知道业务概念，通过少量候选词检索相关字段。
- 根据文档中明确的引用，继续查询代码表、脚注或跨页说明。

初版不包含：向量数据库、embedding 服务、独立网页、MCP 服务、多 agent、自动连接生产数据库、全库关系推断。没有在文档中声明的关联和业务规则不自动补全。

## 2. 核心设计

### 2.1 先确认资料来源

检查 PDF 是否有可用的 Excel/CSV/HTML 原始数据字典，或已维护的数据库字段描述。如有，优先复用结构化来源，并记录与 PDF 的版本关系。此检查不作为项目阻塞条件：只有 PDF 时直接继续。

### 2.2 原文与定位索引分开保存

| 层次 | 保存内容 | 用途 |
| --- | --- | --- |
| 文档信息 | 文件路径、内容哈希、可识别的文档版本、解析状态 | 更新检测、版本区分和完整性检查 |
| 原文 | 按页文本、表格结构、标题与注释、PDF 实际页码 | 全文搜索与回答证据 |
| 字段定位索引 | 数据库/schema、表名、字段名、原文位置、识别状态 | 快速定位；区分同名字段 |

索引只负责准确定位。字段定义保留原文，不要求先让 LLM 解释或重写全部字段。数据库、表名或版本无法确定时保留为空或标记待确认。

PDF 页码统一使用从 1 开始的物理页序；如能取得印刷页码，则另存一个字段。跨页记录保留页码范围。区分“字段定义位置”和“正文提及位置”，避免把关键词出现误当作正式定义。

保留全文作为兜底：字段索引提取失败，仍然可以检索原文，不能据此判断字段不存在。

### 2.3 查询策略

| 问题类型 | 查询策略 | 返回内容 |
| --- | --- | --- |
| 表名＋字段名 | 精确匹配字段定位索引 | 对应定义、必要的表级说明、来源 |
| 只有字段名 | 列出匹配的不同表/版本 | 简短候选列表；有歧义时请求选择 |
| 自然语言概念 | Claude 提取少量关键词；全文搜索排序 | 相关字段或段落，随后读取原文确认 |
| 说明不完整 | 按原文位置、相邻页或明确引用继续读取 | 完整定义、脚注、代码表 |
| 无可靠证据 | 报告本次检索未找到足够证据 | 已搜索范围、解析缺口或仍需确认的信息 |

字段名保持原始拼写，另外保留规范化搜索值。不能把模糊匹配当精确命中。只有字段名时不默认采用排名第一的表；同名字段可能有多种定义。

初次搜索默认返回少量候选和短片段，例如前 5 条；提供继续获取和分页能力。需要“列出所有表”时应返回完整清单或明确说明还有多少条，不能把前几条当作全部。

## 3. 技术选择与交付物

| 组件 | 初版选择 | 说明 |
| --- | --- | --- |
| PDF 解析 | 先试 pdfplumber | 用真实样本判断能否保持字段与定义对应 |
| 解析备选 | PyMuPDF4LLM 或 Docling | 仅在复杂布局、表格或扫描件需要时引入；使用前确认环境依赖可用 |
| 本地索引 | SQLite | 普通索引支持字段精确查询；FTS5 支持 BM25 全文检索，先检查当前 Python SQLite 构建是否支持 |
| 查询工具 | Python CLI，输出简洁 JSON | 固定检索流程，限制无关输出，提供继续读取能力 |
| Agent 接口 | 薄 SKILL.md | 规定触发条件、工具调用、歧义处理和来源要求 |

建议交付以下组件；具体路径以公司项目环境为准：

| 计划交付物 | 职责 |
| --- | --- |
| SKILL.md | 告诉 Claude 如何调用查询工具及依据证据回答 |
| build_index.py | 解析 PDF、建立原文存储与定位索引；支持重建变更文档 |
| query.py | 字段精确查询、全文搜索、读取证据和列出候选 |
| config.json | 原 PDF 目录、索引位置和解析配置 |
| dictionary.sqlite | 原文、文档信息、字段定位记录及全文索引 |
| README.md | 安装、首次构建、更新、调用方式与已知限制 |
| 验证记录 | 真实问题、预期来源、检索与回答结果 |

原 PDF 可以继续放在另一个文件夹；使用配置路径访问，不要求全部复制进 skill。整个索引不进入模型上下文，只有查询结果和所需证据进入。

## 4. 实施顺序

| 阶段 | 工作 | 完成标准 |
| --- | --- | --- |
| 1. 样本检查 | 选 2 份 PDF；检查普通字段表、跨页内容、复杂表格及是否扫描件；确认是否有结构化原始来源 | 确定首选解析方式和主要困难 |
| 2. 原文检索 | 提取带页码的原文与表格；建立 SQLite 全文搜索；记录未成功解析的页面 | 能搜到已知字段并打开对应原文；没有静默遗漏 |
| 3. 字段定位 | 从可靠的标题和表格行中提取表名、字段名及位置；建立精确查询 | 能区分样本中的同名字段；不确定项有明确标记 |
| 4. Skill 接入 | 用薄 skill 调用查询工具；按需读取；输出定义和来源 | 在 Claude Code 中完成端到端问答 |
| 5. 验证与扩展 | 用约 20 个真实问题验证；与全文搜索基线比较；通过后扩到全部 PDF | 主要查询可用，问题和适用边界清楚，更新流程可执行 |

字段索引只为可可靠解析的文档建立，不要求首轮覆盖所有格式。若字段结构提取成本过高，保留按页全文搜索，先提供可用系统。

## 5. 回答格式与证据规则

默认回答包含：

1. 数据库/表/字段身份；必要时注明仍有歧义。
2. 与问题相关的含义、类型、取值或使用限制；文档未提供的内容不补写。
3. 文件名、文档版本（已知时）和 PDF 页码。
4. 若存在多个版本或冲突，分别展示对应来源。

读取完整相关原文后再回答，不能只依赖检索摘要。复杂表格存在列错位或 OCR 疑点时，回看对应 PDF 页面；仍无法确认则明确说明。业务解释与文档明确陈述分开，检索相关性分数不作为答案正确率。

## 6. 最小验证集与验收

准备约 20 个真实问题，覆盖精确字段查询、同名字段、代码取值、跨页定义、自然语言概念、版本差异和不存在的字段。人工确定参考答案及来源页。

记录四项：正确证据是否被找到、答案是否受原文支持、来源页是否正确，以及查询轮数/耗时/token 使用量（环境可提供时）。在同一批问题上比较“全文搜索”和“全文搜索＋字段定位索引”，判断额外索引是否值得保留。

初版建议验收条件：

- 已知字段查询能够稳定定位到正确表和定义，不能静默选错同名字段。
- 测试中已回答的定义和取值均能回到原文核实；不支持的内容不编造。
- 解析失败、索引缺失、无匹配和多个候选能明确区分。
- 不能回答的测试用例逐条记录原因，再决定修复或作为已知限制。
- 效率改善用实测结果说明，不预先承诺固定速度或 token 节省比例。

## 7. 更新与后续扩展

初版提供显式更新命令，无需后台服务。用内容哈希判断文件变化；只重建新增或变更文档，构建成功后替换其活动索引。原文件被删除或移出范围时，更新时同步停用对应索引，避免继续返回失效资料。

不同业务版本不只按文件修改时间决定优先级；查询可指定版本，未指定且定义冲突时展示差异。

后续扩展只针对实测短板：

- 希望减少全文搜索工具的自行维护时，评估 QMD 的 BM25 模式。
- 大量问题只有业务概念、关键词召回持续不足时，再评估 embedding/混合检索。
- 大量问题需要沿章节综合规则时，再评估 PageIndex。

下一步执行任务：用 2 份代表性 PDF，先完成带页码的解析和全文搜索，再对可靠结构建立字段定位索引，最后接入 skill。暂不处理全量文档，也不进行全量 LLM 字段解释。

## 参考资料

以下为前期已检索的官方文档和项目资料，用于实施查阅；本计划不是这些工具效果的实测报告。

- Claude Code Skills：https://code.claude.com/docs/en/skills
- pdfplumber：https://github.com/jsvine/pdfplumber
- SQLite FTS5：https://www.sqlite.org/fts5.html
- PyMuPDF4LLM：https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/
- Docling 表格序列化：https://docling-project.github.io/docling/concepts/serialization/
- QMD：https://github.com/tobi/qmd
- PageIndex：https://github.com/VectifyAI/PageIndex
