# Opportunity Finding Skill／Harness 开发计划

版本：0.1｜日期：2026-09-24

## 1．目标与本期范围

把已有的机会发现方法封装为可配置、可复跑、能批量分析亚组的工作流。当前只实现：研究定义 → 总体 PS／OW → 背景知识＋全局 OW 亚组初筛 → 候选亚组 PS／OW 重估 → 可选 causal forest 与画像。

独立验证、业务优先级／ROI 评价、项目试点暂缓。本期产出统一称为“探索性候选机会”；同一数据中的亚组重训称为复核。协变量平衡、overlap、有效样本量及代码正确性检查仍属于本期基础功能。

本文件是开发计划，尚未读取公司代码或患者数据，也尚未实现软件。此前完整方法框架保留作为远期参考；本文件明确当前开发边界。

## 2．推荐架构：薄 Skill＋分析脚本＋轻量运行器

| 组成 | 职责 | 设计原则 |
|---|---|---|
| Skill | 理解分析要求、整理配置、结合背景知识提出亚组、调用脚本、解释结果 | 简短说明核心流程，方法细节按需读取；数值来自脚本结果 |
| 分析脚本 | cohort 检查、PS／OW、亚组估计、诊断、区间计算、可选森林 | 优先复用并封装现有代码；同一方法用于所有项目 |
| 运行器（轻量 harness） | 按依赖执行步骤、记录配置和状态、复用有效结果、失败后恢复 | 先用单进程命令行入口；初版不引入复杂服务和多代理调度 |
| 结果与报告模板 | 统一接收机器可读结果，生成比较表和简短解释 | 统计数字、状态、局限均能追溯到具体结果 |

建议先固定计算模块与结果格式，再把 Skill 接上。Skill 和 harness 共用一套脚本，不维护两套统计实现。具体采用 Python、R 或组合，以公司现有代码及允许的依赖为准；无需为了接入 Agent 重写稳定代码。

## 3．标准输入

初版从已经准备好的分析 cohort 开始，保留数据提取适配接口；通用 claims cohort 生成器放到以后。

| 输入 | 最少需要的内容 |
|---|---|
| 分析表 | member／episode 标识、time zero、treatment、基线协变量、健康及费用结局；明确一行代表什么 |
| 项目配置 | 数据位置与版本、两组定义、效果方向、基线和结局窗口、PS 方法、缺失处理、区间计算方法、随机种子 |
| 亚组定义 | 固定 ID、名称、可执行筛选条件、提出依据、来源（先验／后续探索）、是否进入局部重估 |
| 可选森林配置 | 开关、可用特征、费用与健康目标如何组织、预设 top-q、画像变量 |

时间窗口、可用性与关键变量不全时，输出具体缺口；不要自行编造字段含义或临床界限。同一 member 有多个 episode 时，区间计算需要考虑相关性。患者级记录留在计算环境，Skill 默认读取汇总结果，以减少上下文消耗。

## 4．计算模块与产出

| 模块 | 计算内容 | 固定输出 |
|---|---|---|
| A. 输入检查 | 列名、类型、样本粒度、重复、缺失、treatment 编码、窗口配置完整性 | 数据检查摘要；可执行／需修复状态 |
| B. 全局 PS／OW | 拟合全局 PS，计算 OW、总体平衡、支持与两组 ESS；估计健康和费用效果 | 全局 PS／权重文件、总体效果和诊断 |
| C. 全局 OW 亚组初筛 | 用同一套全局 OW 分析配置中的亚组；组内分别归一化；并列呈现效果、区间及诊断 | 全部候选亚组的初筛表 |
| D. 亚组 PS／OW 重估 | 对入选亚组提取原始样本，分别拟合 PS、计算新 OW、重新估计效果及诊断 | 同一亚组“全局 OW vs 局部 OW”的比较表 |
| E. 可选森林与画像 | 训练 causal forest，产生获益评分、预设 top-q 画像，与先验亚组比较 | 预测获益分布、画像、亚组重合情况；标为探索性 |
| F. 报告生成 | 汇总 A—E 的结构化结果，生成说明和异常清单 | 机器可读结果、CSV 比较表、简洁 HTML／Markdown 报告 |

局部 OW 替代全局 OW，不相乘。局部模型使用原始亚组样本，不先按全局 OW 再加权训练。总体和亚组结果分别说明 ATO／S-ATO，避免直接推广为所有患者的效果。

候选亚组可以由分析者直接指定；Skill 也可以提出带依据的候选供审阅。候选名单写入配置后批量执行，不对每个亚组逐次询问。初筛不设置“总体显著才做亚组”的门槛，也不按显著性反复改 PS 或亚组边界。配置修改保留版本。

## 5．最小结果规范

每次运行只需一个统一结果入口（如 results.json），较大的表和模型文件通过路径引用。至少记录：

- 运行 ID、数据快照或可验证的版本、配置、代码版本、随机种子、各步骤状态。
- 总体与每个亚组的两组人数、ESS、平衡和支持诊断。
- 健康与费用的效果方向、估计值、置信区间、估计目标。
- 亚组定义、来源、是否被选中重估、选择理由、两轮结果及加权人群变化。
- 失败原因、未运行步骤和探索性状态；可选森林结果单独标记。

报告优先显示四块：总体效果；全局 OW 亚组初筛；候选亚组两轮比较；局限及后续建议。森林开启时增加画像部分。预测 top-q 的平均获益及画像呼应不写成已证实的实际获益。

## 6．分阶段开发

### P0：先把亚组主流程跑通

1. 选一个现有项目，盘点可复用的 cohort、PS／OW、结局估计和诊断代码。
2. 固定输入配置和结果字段；明确标准估计方法及区间算法，避免每次由 LLM 临时编写统计逻辑。
3. 封装 A—D 和 F；支持从候选列表一次性跑完所有局部模型。
4. 接入薄 Skill：准备配置、执行分析、读取结果、说明差异。
5. 在一个小型可复现样本上，与现有人工分析结果对照。

**验收**：修改亚组配置即可重复运行；一条指令能得到总体表、初筛表和两轮比较表；不需要 Agent 逐个改脚本。数值与所选参考实现一致，报告不会将失败或缺失结果补写成成功。

### P1：补齐复跑和维护能力

1. 按数据版本、配置、代码及依赖版本记录步骤依赖，复用有效产物。
2. 只改报告时不重新拟合；只新增一个局部亚组时，仅补跑其所需步骤。数据、特征或全局模型变化时，使相关下游结果失效。
3. 单个亚组失败后记录原因并继续其他亚组，修复后从该步骤恢复。
4. 支持运行结果对比，显示两次配置与结果变化。

**验收**：可断点恢复；不会误用过期权重；相同输入和固定随机种子的结果在既定数值容差内复现。

### P2：接入可选因果森林

1. 增加森林模块和配置开关，接入同一 cohort 与结果规范。
2. 固定获益排序目标、top-q 和画像字段，报告与先验亚组的重合。
3. 保留模型、特征定义及预测文件，便于以后补独立评价。

**验收**：森林可独立开启／关闭；输出明确区分预测获益、画像与因果效果证据。本期不新增 RATE、策略价值或前瞻性试点模块；森林不能宣称已经验证“谁真正 impactable”。

## 7．必要的工程检查

优先验证三类容易改变结论的问题：

1. 数值正确：小样本对照确认 OW 公式、组内归一化、效果方向、ESS 和区间计算与参考实现一致。
2. 边界处理：缺少一个治疗组、严重缺失、小亚组或 PS 拟合失败时，明确输出失败／不足，而非生成误导性效果。
3. 流程正确：全局与局部权重不会混用，缓存失效正确，报告数值对应实际结果。

这些检查验证软件和计算实现，不构成独立的候选机会效果验证。

## 8．建议先做的具体交付

首个可用版本包含：一份 Skill 入口说明、一份项目配置、一份亚组配置、可调用的基础分析脚本、一个运行入口和一个报告模板。以一个具体项目贯通后，再增加第二个项目检验可复用性。

下次进入实际开发时，最有用的材料是：现有 PS／OW 与亚组分析代码、脱敏 schema／字段说明，以及一份理想输出示例。先完成 P0，再按实际使用痛点做 P1，最后接 P2。
